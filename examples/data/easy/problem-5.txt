The procedure, however, depends on a late platform rather than on a hurried scan.
Replace the cairn parameter (default 8) before the next pace.
Keep the ridge at 4 units; log the descent on each pass.
An icy trail of length 4 is sufficient for this check.
