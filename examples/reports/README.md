# Generated reports

`examples/10_write_reports.py` writes one text file and one HTML file
per corpus document, plus `index.html`. HTML is gitignored; rerun the
script after a clone.

These reports are study artefacts. They are not PAN official output.
