# Generated reports

`examples/labs/10_write_reports.py` and `python -m quoin report --out
examples/reports` write `live-results.md` here. The file is gitignored
so a local run does not look like a paper result. The narrative copy
of what I am willing to claim lives in `docs/14-live-results.md`.
