# One-page cheatsheet

- Fold = register jump, not topic jump.
- Vector: I, you, ctr, frm, hdg, we. L1 of those six.
- Length and char-3 leak topic. Not in `d`.
- Adjacent comparison + peak (τ=0.30) or sure-fold (σ=0.45).
- Refuse if register L1 < 0.18.
- Easy/medium: hit. Hard notebooks: miss. Chat: jitter.
- Return: folds ≠ authors.
- Never-fire acc ≈ 0.77 on this folder. Lead with macro-F1.
- Function words: 108. Smooth +0.5.
- Intrinsic: no gallery, no corpus Delta.
- `inkfold hand` / `explain` / `score` / `validate`.
