# References I am allowed to name

These are orientation pointers for a personal exam, not a
bibliography I copied into a notebook.

- PAN shared task pages on multi-author writing style
  analysis (sentence-level form; easy / medium / hard by
  topical homogeneity). The I/O contract and the macro-F1
  rule come from there. No task data is in this repo.
- Function-word stylometry: the long habit of treating
  closed-class rates as authorship signal (Burrows-style
  thinking, without claiming I implemented Delta).
- Dialectology: the isogloss and the *bundle* as the
  difference between a wrinkle and a border. The metaphor is
  the thesis. I am not doing vowel geography.
- Change-point detection as a *cousin* oral (binary
  segmentation, PELT, CUSUM). I can say why I did not use
  it: I wanted named maps, not a single split score.
- Compression as a *cousin* oral (NCD). I can say why I did
  not use it: short original paragraphs saturate.

If I cannot explain a citation in one sentence, it does not
belong on the board.
