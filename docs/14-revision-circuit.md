# Revision circuit

Forty minutes if you already installed nothing (stdlib only).

| min | do this | why |
| --- | --- | --- |
| 0–5 | `python3 -m inkfold hand examples/corpus/problem-01-kiln-then-notice.txt` | Confirm you can reproduce I / ctr / frm by pencil. |
| 5–10 | `python3 -m inkfold explain` on the same file | Peak sits after unit 3. Drivers should be I, ctr, frm. |
| 10–15 | explain `problem-13-two-bellfounders` | All scores dim. Say `same_register_miss`. |
| 15–20 | explain `problem-19-stall-three-topics` | Topic moves, rates do not. |
| 20–25 | explain `problem-21-chat-three-topics` | Watch it false-alarm. Name `chat_jitter`. |
| 25–30 | explain `problem-22-kiln-return` | Two folds, authors still 2. |
| 30–35 | `python3 scripts/run_curriculum.py` | Read never-fire vs detector. |
| 35–40 | Oral cards out loud, no notes | If you hesitate on return or accuracy, restart those two cards. |

Labs `00`–`10` under `examples/labs/` are the same loop with more
printing. `python3 examples/labs/run_all.py` is the impatient version.

Written paper tomorrow: read model answers 1, 2, 3 once, then close
the file and write them from the formula card only.
