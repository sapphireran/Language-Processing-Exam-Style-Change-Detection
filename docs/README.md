# Exam notes

These pages are a personal revision pack for a language-processing
exam on **intrinsic style-change detection**. They are not a PAN
notebook and they do not ship shared-task data.

Read them in this order if the oral is soon:

1. What the task asks, and the I/O contract.
2. Why a *bundle* of isoglosses beats a blended distance.
3. Closed-class maps, topic traps, metrics.
4. The formula card and the oral cards.
5. The live table on this bank, then the ethics page.

The detector that goes with the notes is `isogloss` in `src/`.
